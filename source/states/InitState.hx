package;

/*
    Handles initialization settings and so on
    Currently empty bc this is an empty project, duh
*/

class InitState extends FlxState
{
    override function create()
    {
        super.create();
    }
}